{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - MERN full-stack application",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "mongo": "powershell -ExecutionPolicy Bypass -File scripts/start-mongo.ps1",
    "dev": "echo Run: npm run mongo, then dev:backend and dev:frontend in separate terminals"
  },
  "engines": {
    "node": ">=18"
  }
}
